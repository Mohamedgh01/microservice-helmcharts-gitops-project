replicaCount: 1

image:
  repository: mohamedgharbii/frontend
  tag: latest
  pullPolicy: Always

service:
  type: NodePort
  port: 30080  # Kubernetes choisira automatiquement nodePort

resources:
  limits:
    memory: "512Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"
